import React, { useRef } from "react";


const Hero = () => (
  
  <div className="hero my-5 text-center" data-testid="hero">
   <div className="hero-title" data-testid="hero-lead"></div>
  </div>
);

export default Hero;
